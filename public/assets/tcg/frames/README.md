Reserved for future repo-served TCG frame overlays.

Vite serves files in `public/` from the site root, so files placed here are available at:

- `/assets/tcg/frames/<filename>`

The current Owner preview uses CSS frame styling only. Do not add uploads or Supabase Storage for this phase.
