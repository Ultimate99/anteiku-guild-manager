# Server-only Supabase Edge Function secrets.
# Copy to supabase/functions/.env for local testing and never commit real values.

PUSH_SENDER_SECRET=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
